<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".SignUpActivity">


    <EditText
        android:id="@+id/user_editText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:hint="@string/signup_activity_login"
        />

    <EditText
        android:id="@+id/email_editText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/user_editText"
        android:layout_marginTop="@dimen/default_margin_top"
        android:hint="@string/signup_activity_email"
        android:inputType="textEmailAddress"
        />

    <EditText
        android:id="@+id/password_editText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/email_editText"
        android:layout_marginTop="@dimen/default_margin_top"
        android:hint="@string/signup_activity_password"
        android:inputType="textPassword"
        />

    <EditText
        android:id="@+id/password_confirm_editText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/password_editText"
        android:layout_marginTop="@dimen/default_margin_top"
        android:hint="@string/signup_activity_password_confirm"
        android:inputType="textPassword"
        />

    <Button
        android:id="@+id/signup_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/password_confirm_editText"
        android:layout_marginTop="@dimen/default_margin_top"
        android:text="@string/signup_activity_signup"/>

</RelativeLayout>
