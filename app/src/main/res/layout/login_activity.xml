<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                tools:context=".LoginActivity">

    <EditText
        android:id="@id/login_editText_login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="150dp"
        android:hint="@string/loginActivity_login"/>

    <EditText
        android:id="@id/login_editText_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_editText_login"
        android:layout_marginTop="@dimen/default_margin_top"
        android:hint="@string/loginActivity_password"
        android:inputType="textPassword"/>

    <Button
        android:id="@id/login_button_connect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_editText_password"
        android:layout_marginTop="@dimen/default_margin_top"
        android:text="@string/loginActivity_connect"/>

    <TextView
        android:id="@id/login_textView_noAccount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_button_connect"
        android:layout_centerInParent="true"
        android:layout_marginTop="@dimen/default_margin_top"
        android:text="@string/loginActivity_noAccountYet"
        android:textSize="@dimen/default_text_size"/>

    <Button
        android:id="@id/login_button_signup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_textView_noAccount"
        android:layout_marginTop="@dimen/default_margin_top"
        android:text="@string/loginActivity_signup"/>

</RelativeLayout>
